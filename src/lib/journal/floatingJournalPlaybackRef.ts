/** Set on artifact detail (YouTube) when the iframe player can report time; read by the global journal shell. */
export const floatingJournalPlaybackRef: { current: (() => number | null) | null } = { current: null };

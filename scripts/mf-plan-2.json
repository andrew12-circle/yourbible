{"src/hooks/useMorningConversationEntry.ts": ["5936399fbc4113c4d922da6671560fcb955619174af3ddf36f097cd5a048a73a", [[0, 0, "import { formatSupabaseError } from \"@/lib/supabase/errors\";\n"], [34, 35, "      setError(formatSupabaseError(e));\n"], [56, 57, "        setError(formatSupabaseError(e));\n"], [65, 67, "    let active = true;\n    setEntryId(null);\n    setPreview(null);\n    void findMorningConversationEntry(userId, reviewDate).then(async (id) => {\n      if (!active || !id) return;\n"], [68, 70, "      await refreshPreview(id);\n    }).catch((e) => { if (active) setError(formatSupabaseError(e)); });\n    return () => { active = false; };\n"]]], "src/hooks/useMorningFormulaTimer.ts": ["ae87022647560e1923ffcb7d6dde88e57ce4043918967ea0fb547873945e99f2", [[3, 3, "  extendFormulaStepTimer,\n"], [21, 21, "  const [stepExtraMs, setStepExtraMs] = useState(() => loadFormulaStepTimer().stepExtraMs ?? 0);\n"], [29, 29, "      setStepExtraMs(persisted.stepExtraMs ?? 0);\n"], [33, 33, "    setStepExtraMs(0);\n"], [51, 51, "        stepExtraMs,\n"], [52, 53, "    [steps, stepIndex, durationMin, stepStartedAt, activeStepKey, stepExtraMs, tick],\n"], [60, 60, "      setStepExtraMs(0);\n"], [68, 68, "    addFiveMinutes: () => { if (stepKey && timed) setStepExtraMs(extendFormulaStepTimer(stepKey, 5 * 60 * 1000)); },\n"]]], "src/hooks/useMorningRitualDraft.ts": ["0d29903a2b276a491984e33b72224afb4e4ccba6959fbf66eb375d3347e4edd0", [[17, 17, "  const latest = useRef({ userId, input, enabled: opts?.enabled !== false });\n  latest.current = { userId, input, enabled: opts?.enabled !== false };\n  useEffect(() => {\n    const flush = () => {\n      if (!userId || latest.current.userId !== userId || !latest.current.input || !latest.current.enabled) return;\n      try { saveMorningRitualDraft(userId, latest.current.input); } catch { /* Keep in-memory state. */ }\n    };\n    window.addEventListener(\"pagehide\", flush);\n    return () => { window.removeEventListener(\"pagehide\", flush); flush(); };\n  }, [userId]);\n"]]], "src/lib/bible/readerNavigation.ts": ["30501d57b9b7056bc2a92dee0e00e35e38d7e2f52344127eb0ae2cff7121726f", [[23, 24, "    returnLabel: \"Return to Morning Formula\",\n"], [29, 33, "  try {\n    sessionStorage.setItem(SESSION_KEY, JSON.stringify({ to: state.returnTo, label: state.returnLabel ?? \"Back\" }));\n  } catch { /* The URL still carries the return destination. */ }\n"], [54, 54, "  if (typeof window !== \"undefined\") {\n    const query = new URLSearchParams(window.location.search);\n    if (query.get(\"returnTo\") === MORNING_FORMULA_SCRIPTURE_RETURN) {\n      return { to: MORNING_FORMULA_SCRIPTURE_RETURN, label: \"Return to Morning Formula\" };\n    }\n  }\n"], [60, 60, "\n/** Portable reader link; never depends on opener storage or a React history entry. */\nexport function morningFormulaReaderHref(readerHref: string, popout = false): string {\n  const href = readerHref.startsWith(\"/\") && !readerHref.startsWith(\"//\") && !readerHref.includes(\"\\\\\") ? readerHref : \"/reader\";\n  const url = new URL(href, \"https://yourbible.invalid\");\n  url.searchParams.set(\"returnTo\", MORNING_FORMULA_SCRIPTURE_RETURN);\n  if (popout) url.searchParams.set(\"formulaPopout\", \"1\");\n  return `${url.pathname}${url.search}${url.hash}`;\n}\n"]]], "src/lib/livingHope/morningConversationJournal.ts": ["4fec26fdd30ba30650235cfa15c5e3a25c4828c477b2e72ed3ab711140e935c5", [[0, 0, "import { replaceMorningSection, updateMorningFormulaEntry } from \"./morningFormulaJournalBody\";\n"], [108, 129, "  return replaceMorningSection(existingBody, MORNING_CONVERSATION_WORSHIP_HEADING, worshipText.trim() || WORSHIP_PLACEHOLDER);\n"], [133, 154, "  return replaceMorningSection(existingBody, MORNING_CONVERSATION_HEART_HEADING,\n    heartText.trim() || \"Talk, type, dictate, sketch, or add photos and video — whatever is honest today.\",\n    MORNING_CONVERSATION_LISTENING_HEADING);\n"], [156, 166, "export async function syncWorshipToConversationEntry(userId: string, entryId: string, worshipText: string): Promise<void> {\n  await updateMorningFormulaEntry(userId, entryId, (body) => mergeWorshipIntoConversationBody(body, worshipText));\n"], [168, 195, "export async function syncHeartToConversationEntry(userId: string, entryId: string, heartText: string): Promise<void> {\n  await updateMorningFormulaEntry(userId, entryId, (body) => mergeHeartIntoConversationBody(body, heartText));\n"], [205, 227, "export function mergeThanksgivingIntoConversationBody(existingBody: string, lists: ThanksgivingLists): string {\n  return replaceMorningSection(existingBody, MORNING_CONVERSATION_THANKSGIVING_HEADING,\n    formatThanksgivingJournalBody(lists) ?? \"\");\n"], [234, 235, "  const { data, error } = await supabase\n"], [243, 243, "  if (error) throw error;\n"], [246, 247, "const pendingEntries = new Map<string, Promise<{ entryId: string; created: boolean }>>();\n\n/** One in-flight request per user/day, with a database lock across tabs/devices. */\n"], [251, 285, "  const key = `${userId}:${reviewDate}`;\n  const pending = pendingEntries.get(key);\n  if (pending) return pending;\n  const request = (async () => {\n    const existingId = await findMorningConversationEntry(userId, reviewDate);\n    if (existingId) return { entryId: existingId, created: false };\n    const journalId = await getDefaultJournalId(userId);\n    const context = await getCurrentContext().catch(() => ({}));\n    const { data, error } = await supabase.rpc(\"ensure_morning_formula_entry\", {\n      p_review_date: reviewDate,\n      p_journal_id: journalId,\n      p_title: conversationEntryTitle(reviewDate),\n      p_body: MORNING_CONVERSATION_BODY_TEMPLATE,\n      p_context: JSON.parse(JSON.stringify(context)),\n    });\n    if (error) throw error;\n    if (!data?.[0]?.entry_id) throw new Error(\"Couldn't create today's journal. Try again.\");\n    return { entryId: data[0].entry_id, created: data[0].created };\n  })();\n  pendingEntries.set(key, request);\n  try { return await request; } finally { if (pendingEntries.get(key) === request) pendingEntries.delete(key); }\n"], [288, 314, "export async function syncThanksgivingToConversationEntry(userId: string, entryId: string, lists: ThanksgivingLists): Promise<void> {\n  await updateMorningFormulaEntry(userId, entryId, (body) => mergeThanksgivingIntoConversationBody(body, lists));\n"]]], "src/lib/livingHope/morningFormulaTimer.ts": ["bd28fd4f8e7ffebc83aa1240d824d13983fa2c73b26f8300ae034ae8704abbae", [[16, 17, "  thanksgiving: 6,\n"], [34, 34, "  stepExtraMs?: number;\n"], [60, 60, "      stepExtraMs: typeof parsed.stepExtraMs === \"number\" && Number.isFinite(parsed.stepExtraMs) ? Math.max(0, parsed.stepExtraMs) : 0,\n"], [68, 69, "  try { localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); } catch { /* Pacing still works in memory. */ }\n"], [77, 78, "  writePersistedState({ ...state, durationMin, stepKey: null, stepStartedAt: null, stepExtraMs: 0 });\n"], [147, 147, "  stepExtraMs = 0,\n"], [151, 152, "  const stepBudgetMs = stepKey ? (budgets[stepKey] ?? 0) + (stepKey === activeStepKey ? Math.max(0, stepExtraMs) : 0) : 0;\n"], [175, 176, "  writePersistedState({ ...state, stepKey, stepStartedAt: now, stepExtraMs: 0 });\n"], [179, 182, "export function loadFormulaStepTimer(): Pick<FormulaTimerPersistedState, \"stepKey\" | \"stepStartedAt\" | \"stepExtraMs\"> {\n  const { stepKey, stepStartedAt, stepExtraMs } = readPersistedState();\n  return { stepKey, stepStartedAt, stepExtraMs };\n"], [187, 187, "\nexport function extendFormulaStepTimer(stepKey: string, additionalMs: number): number {\n  const state = readPersistedState();\n  const current = state.stepKey === stepKey ? state.stepExtraMs ?? 0 : 0;\n  if (state.stepKey !== stepKey) return 0;\n  const extra = current + (Number.isFinite(additionalMs) ? Math.max(0, additionalMs) : 0);\n  writePersistedState({ ...state, stepExtraMs: extra });\n  return extra;\n}\n"]]], "src/lib/livingHope/morningReviewJournal.ts": ["83adc8c8c2cc861b26c913758b91d00a81421bab965daea874fc2d1c568a9829", [[0, 0, "import { getOrCreateMorningConversationEntry } from \"./morningConversationJournal\";\nimport { mergeMorningReviewBody, updateMorningFormulaEntry } from \"./morningFormulaJournalBody\";\n"], [2, 3, "import { type EntryLinkInput } from \"@/lib/journal/links\";\n"], [231, 232, "    .in(\"entry_kind\", [\"morning_conversation\", MORNING_REVIEW_ENTRY_KIND])\n"], [233, 234, "    .order(\"created_at\", { ascending: true })\n"], [265, 313, "  const { body, summary, verseRefs } = buildMorningReviewJournalContent(ctx);\n  const { entryId } = await getOrCreateMorningConversationEntry(userId, ctx.reviewDate);\n  await updateMorningFormulaEntry(userId, entryId, (existing) => mergeMorningReviewBody(existing, body), {\n    summary, extraTags: [morningReviewTag(ctx.reviewDate), \"living-hope\", \"morning-formula\"],\n  });\n  const manifesto = ctx.workbook?.manifesto.length && ctx.manifestoIndex != null\n    ? ctx.workbook.manifesto[ctx.manifestoIndex % ctx.workbook.manifesto.length]?.text : null;\n"], [318, 323, "  const links = [...verseLinks(verseRefs), ...beliefLinks(beliefIds), ...entryLinks(linkedEntryIds)];\n  const { data: existingLinks, error: linksError } = await supabase.from(\"journal_entry_links\")\n    .select(\"target_kind,target_ref\").eq(\"entry_id\", entryId).eq(\"user_id\", userId);\n  if (linksError) throw linksError;\n  const missing = links.filter((link) => !(existingLinks ?? []).some((existing) =>\n    existing.target_kind === link.kind && JSON.stringify(existing.target_ref) === JSON.stringify(link.ref)));\n  if (missing.length) {\n    const { error } = await supabase.from(\"journal_entry_links\").insert(missing.map((link) => ({\n      user_id: userId, entry_id: entryId, target_kind: link.kind, target_ref: link.ref,\n    })));\n    if (error) throw error;\n  }\n"], [345, 346, "    const { error } = await supabase\n"], [350, 350, "    if (error) throw error;\n"]]]}
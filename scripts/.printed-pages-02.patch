diff --git a/scripts/test-bible-reader.mjs b/scripts/test-bible-reader.mjs
index d6b7146..1ff0169 100644
--- a/scripts/test-bible-reader.mjs
+++ b/scripts/test-bible-reader.mjs
@@ -0,0 +1 @@
+import { renderedVerseFragments, verifyConsecutiveFragments, verifyFragmentWords } from "./reader-browser-text.mjs";
@@ -55,4 +56,2 @@ try{
- const identity=async()=>{
-  const wrong=await verses().evaluateAll(nodes=>nodes.flatMap(node=>{const id=node.getAttribute('data-verse-id');const[,book,chapter,verse]=id.split(':');return node.textContent.includes('FIXTURE '+book+' '+chapter+' '+verse+'.')?[]:[{id,text:node.textContent.slice(0,130)}]}));
-  assert.deepEqual(wrong,[],'Text belongs to a different verse identity');
- };
+ const lookupVerse=(book,chapter,number)=>({number,text:'FIXTURE '+book+' '+chapter+' '+number+'. '+('Synthetic reading text with several words for reliable page measurement. ').repeat(3)});
+ const identity=async()=>verifyFragmentWords(await renderedVerseFragments(page),lookupVerse);
@@ -73 +72 @@ try{
-  for(const id of ids){const[,book,ch,v]=id.split(':');if(book==='Jhn'&&ch==='3')collected.push(Number(v))}
+  collected.push(...(await renderedVerseFragments(page)).filter(row=>row.id.includes(':Jhn:3:')));
@@ -77 +76 @@ try{
- assert.deepEqual(collected,Array.from({length:42},(_,i)=>i+1),'A full chapter was duplicated, omitted, or out of order across page turns');record('Every synthetic verse occurs exactly once in order across paged Scripture and artwork');
+ assert.deepEqual(verifyConsecutiveFragments(collected,lookupVerse,{complete:true}).map(id=>Number(id.split(':').at(-1))),Array.from({length:42},(_,i)=>i+1),'A full chapter was duplicated, omitted, or out of order across page turns');record('Every synthetic verse occurs exactly once in order across paged Scripture and artwork');
diff --git a/src/hooks/useReaderOverflowRecovery.test.tsx b/src/hooks/useReaderOverflowRecovery.test.tsx
index cbc65c7..7f88922 100644
--- a/src/hooks/useReaderOverflowRecovery.test.tsx
+++ b/src/hooks/useReaderOverflowRecovery.test.tsx
@@ -31 +31 @@ describe("book-page overflow recovery", () => {
-  it("requires an explicit choice for an oversized unit and clears it when the unit fits", () => {
+  it("repaginates even one long verse instead of requiring a different reading mode", () => {
@@ -36,2 +36,2 @@ describe("book-page overflow recovery", () => {
-    expect(result.current).toEqual({left: "oversized"});
-    expect(correction).not.toHaveBeenCalled();
+    expect(result.current).toEqual({});
+    expect(correction).toHaveBeenCalledWith(3);
diff --git a/src/hooks/useReaderOverflowRecovery.ts b/src/hooks/useReaderOverflowRecovery.ts
index 5628e75..79ff415 100644
--- a/src/hooks/useReaderOverflowRecovery.ts
+++ b/src/hooks/useReaderOverflowRecovery.ts
@@ -6 +6 @@ type PageProblems = Partial<Record<"left" | "right", "oversized" | "unresolved">
-/** Re-paginate ordinary overflow. A truly oversized passage needs explicit reader choice. */
+/** Re-paginate ordinary overflow. Word-level page cuts also allow a long verse to span physical pages. */
@@ -37 +37 @@ export function useReaderOverflowRecovery(
-        if (verseCount > 1 && canCorrect && requestCorrection) maximumOverflow = Math.max(maximumOverflow, result.overflowPx);
+        if (canCorrect && requestCorrection) maximumOverflow = Math.max(maximumOverflow, result.overflowPx);
diff --git a/src/hooks/useReaderPosition.ts b/src/hooks/useReaderPosition.ts
index fb000a1..c3a1946 100644
--- a/src/hooks/useReaderPosition.ts
+++ b/src/hooks/useReaderPosition.ts
@@ -0,0 +1 @@
+import { readerStreamUnitId } from "@/lib/bible/readerWindowFlow";
@@ -7,2 +8 @@ export function readerUnitAnchor(unit: ReaderStreamUnit): ReaderAnchor {
-  const prefix = `${unit.bookAbbr}|${unit.chapter}`;
-  return { id: unit.kind === "verse" ? `${prefix}|v${unit.verse.number}` : unit.kind === "plate" ? `${prefix}|art:${unit.plate.id}` : `${prefix}|heading`, bookAbbr: unit.bookAbbr, chapter: unit.chapter, verse: unit.kind === "verse" ? unit.verse.number : undefined };
+  return { id: readerStreamUnitId(unit), bookAbbr: unit.bookAbbr, chapter: unit.chapter, verse: unit.kind === "verse" ? unit.verse.number : undefined };
diff --git a/src/index.css b/src/index.css
index e742d5f..622caa1 100644
--- a/src/index.css
+++ b/src/index.css
@@ -1352 +1352 @@
-  [data-bible-reader] [data-reading-area] .holman-study-stack > div:first-child:not(:has([class*="scripture-columns"])) {
+  [data-reading-area] .holman-study-stack > div:first-child:not(:has([class*="scripture-columns"])) {
@@ -1356 +1356 @@
-  [data-bible-reader] [data-reading-area]:not(:has([data-bible-scroll])) .scripture-paragraph {
+  [data-reading-area]:not(:has([data-bible-scroll])) .scripture-paragraph {
diff --git a/src/lib/bible/paginateReaderStream.ts b/src/lib/bible/paginateReaderStream.ts
index 9eec529..484c8f9 100644
--- a/src/lib/bible/paginateReaderStream.ts
+++ b/src/lib/bible/paginateReaderStream.ts
@@ -41,2 +41,2 @@ export function paginateReaderStream(
-    // A title never owns an empty text page. An irreducible unit is preserved;
-    // the reader offers explicit continuous reading, never silently scrolls a page.
+    // A title never owns an empty text page. In page mode, Scripture units
+    // are measured word fragments; long verses continue onto later pages.
diff --git a/src/lib/bible/readerColumnLayout.test.ts b/src/lib/bible/readerColumnLayout.test.ts
index 88f72fb..3fcd6e0 100644
--- a/src/lib/bible/readerColumnLayout.test.ts
+++ b/src/lib/bible/readerColumnLayout.test.ts
@@ -11 +11 @@ describe("readerColumnLayout", () => {
-    expect(effectiveReaderColumnLayout({})).toBe("single");
+    expect(effectiveReaderColumnLayout({})).toBe("double");
diff --git a/src/lib/bible/readerColumnLayout.ts b/src/lib/bible/readerColumnLayout.ts
index a8c50c2..d742118 100644
--- a/src/lib/bible/readerColumnLayout.ts
+++ b/src/lib/bible/readerColumnLayout.ts
@@ -12 +12 @@ export function readReaderColumnLayout(): ReaderColumnLayout {
-  return "single";
+  return "double";
@@ -24 +24 @@ export function effectiveReaderColumnLayout(options: {
-  return options.stored ?? "single";
+  return options.stored ?? "double";
diff --git a/src/lib/bible/readerColumnMeasure.test.ts b/src/lib/bible/readerColumnMeasure.test.ts
index cc483e5..fa5f325 100644
--- a/src/lib/bible/readerColumnMeasure.test.ts
+++ b/src/lib/bible/readerColumnMeasure.test.ts
@@ -39 +39 @@ describe("readerColumnMeasure", () => {
-    ).toBe(444);
+    ).toBe(508);
@@ -47 +47 @@ describe("readerColumnMeasure", () => {
-    ).toBe(428);
+    ).toBe(492);
@@ -55 +55 @@ describe("readerColumnMeasure", () => {
-    ).toBe(524);
+    ).toBe(588);
@@ -67 +67 @@ describe("readerColumnMeasure", () => {
-    ).toBe(412);
+    ).toBe(504);
@@ -76 +76 @@ describe("readerColumnMeasure", () => {
-    ).toBe(396);
+    ).toBe(488);
@@ -85 +85 @@ describe("readerColumnMeasure", () => {
-    ).toBe(396);
+    ).toBe(488);
@@ -94 +94 @@ describe("readerColumnMeasure", () => {
-    ).toBe(492);
+    ).toBe(584);
@@ -116 +116 @@ describe("readerColumnMeasure", () => {
-    ).toBe(420);
+    ).toBe(512);
@@ -120 +120 @@ describe("readerColumnMeasure", () => {
-    expect(paginatorMeasureLimitPx(568)).toBe(536);
+    expect(paginatorMeasureLimitPx(568)).toBe(564);
@@ -141 +141 @@ describe("readerColumnMeasure", () => {
-    expect(col.style.height).toBe("88px");
+    expect(col.style.height).toBe("116px");
@@ -234 +234 @@ describe("readerColumnMeasure", () => {
-  it("paginatorSpreadPaneLimitPx reserves extra spread pane guard", () => {
+  it("spread measurement does not subtract another set of page margins", () => {
@@ -252 +252 @@ describe("readerColumnMeasure", () => {
-    expect(spread.stackContentHeightPx).toBe(paginatorSpreadPaneLimitPx(524));
+    expect(spread.stackContentHeightPx).toBe(paginatorSpreadPaneLimitPx(588));
diff --git a/src/lib/bible/readerColumnMeasure.ts b/src/lib/bible/readerColumnMeasure.ts
index 4e7694a..6a31c79 100644
--- a/src/lib/bible/readerColumnMeasure.ts
+++ b/src/lib/bible/readerColumnMeasure.ts
@@ -21 +21 @@ export interface ScriptureColumnMeasureOptions {
-export const READER_LIVE_COLUMN_SAFETY_PX = 32;
+export const READER_LIVE_COLUMN_SAFETY_PX = 4;
@@ -31,2 +31,2 @@ export function paginatorMeasureLimitPx(contentHeightPx: number): number {
-/** Extra slack reserved per spread pane during paginator measurement. */
-export const READER_SPREAD_PANE_EXTRA_GUARD_PX = 72;
+/** Faces are measured at their real available height; do not subtract a second page margin. */
+export const READER_SPREAD_PANE_EXTRA_GUARD_PX = 0;
@@ -51 +51 @@ export function readerScriptureColumnsHeightPx(
-export const READER_COLUMN_CLIP_GUARD_PX = 36;
+export const READER_COLUMN_CLIP_GUARD_PX = 2;
@@ -54 +54 @@ export const READER_COLUMN_CLIP_GUARD_PX = 36;
-export const READER_PAGE_NAV_CLEARANCE_PX = 40;
+export const READER_PAGE_NAV_CLEARANCE_PX = 10;
diff --git a/src/lib/bible/readerScriptureRender.tsx b/src/lib/bible/readerScriptureRender.tsx
index f22b976..cdb607b 100644
--- a/src/lib/bible/readerScriptureRender.tsx
+++ b/src/lib/bible/readerScriptureRender.tsx
@@ -0,0 +1 @@
+import { readerVerseFragment } from "./readerVerseFragments";
@@ -271 +272,2 @@ export function renderScriptureParagraphNodes(
-      const heading = first != null ? headingMap.get(first) : undefined;
+      const continuesVerse = (readerVerseFragment(group.verses[0])?.start ?? 0) > 0;
+      const heading = first != null && !continuesVerse ? headingMap.get(first) : undefined;
@@ -289 +291 @@ export function renderScriptureParagraphNodes(
-          isContinuation={group.isContinuation}
+          isContinuation={group.isContinuation || continuesVerse}
@@ -295 +297 @@ export function renderScriptureParagraphNodes(
-              paragraphIsContinuation: group.isContinuation,
+              paragraphIsContinuation: group.isContinuation || continuesVerse,
diff --git a/src/lib/bible/readerStream.ts b/src/lib/bible/readerStream.ts
index 0ef5b5f..8385da1 100644
--- a/src/lib/bible/readerStream.ts
+++ b/src/lib/bible/readerStream.ts
@@ -0,0 +1 @@
+import { appendReaderStreamVerse, type ReaderTextRange } from "./readerVerseFragments";
@@ -27 +28 @@ export type ReaderStreamUnit =
-  | { kind: "verse"; bookAbbr: string; bookName: string; chapter: number; verse: PassageVerse };
+  | { kind: "verse"; bookAbbr: string; bookName: string; chapter: number; verse: PassageVerse; verseRange?: ReaderTextRange };
@@ -117 +118 @@ export function areSameStreamSplits(a: number[], b: number[]): boolean {
-export const READER_PAGINATOR_SPLIT_REVISION = 20;
+export const READER_PAGINATOR_SPLIT_REVISION = 21;
@@ -572 +573 @@ export function sliceReaderStreamRange(
-    current.verses.push(unit.verse);
+    appendReaderStreamVerse(current.verses, unit);
@@ -697 +698 @@ export function verseGroupsFromStreamRange(
-    current.verses.push(unit.verse);
+    appendReaderStreamVerse(current.verses, unit);
